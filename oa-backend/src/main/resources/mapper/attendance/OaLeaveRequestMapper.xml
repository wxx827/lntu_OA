<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.module.attendance.mapper.OaLeaveRequestMapper">
    
    <select id="selectByUserId" resultType="com.oa.module.attendance.entity.OaLeaveRequest">
        SELECT * FROM OA_LEAVE_REQUEST 
        WHERE USER_ID = #{userId}
        ORDER BY CREATE_TIME DESC
    </select>
    
    <select id="selectPendingByApproverId" resultType="com.oa.module.attendance.entity.OaLeaveRequest">
        SELECT * FROM OA_LEAVE_REQUEST 
        WHERE APPROVER_ID = #{approverId} AND STATUS = 'PENDING'
        ORDER BY CREATE_TIME ASC
    </select>
    
</mapper>
