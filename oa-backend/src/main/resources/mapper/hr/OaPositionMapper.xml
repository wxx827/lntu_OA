<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.module.hr.mapper.OaPositionMapper">
    
    <select id="selectPositionListWithDept" resultType="com.oa.module.hr.entity.OaPosition">
        SELECT p.*, d.DEPT_NAME
        FROM OA_POSITION p
        LEFT JOIN OA_DEPARTMENT d ON p.DEPT_ID = d.DEPT_ID
        WHERE p.STATUS = 'ACTIVE'
        ORDER BY p.CREATE_TIME DESC
    </select>
    
    <select id="selectByDeptId" resultType="com.oa.module.hr.entity.OaPosition">
        SELECT * FROM OA_POSITION 
        WHERE DEPT_ID = #{deptId} AND STATUS = 'ACTIVE'
        ORDER BY POSITION_LEVEL
    </select>
    
</mapper>
